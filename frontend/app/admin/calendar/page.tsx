import type { NextPage } from "next";
import AdminCalendar from "../../../components/adminCalendar"; // Adjust the path as needed

const AdminDashboard: NextPage = () => {
  return (
    <div>
      <AdminCalendar />
    </div>
  );
};

export default AdminDashboard;
